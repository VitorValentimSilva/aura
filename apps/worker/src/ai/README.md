# ai/

Pasta reservada para o módulo NestJS que vai expor `aura-ai` via injeção de dependência (um provider concreto de `AiProvider` registrado por trás da interface). Ainda vazio — `aura-ai` só tem a interface, sem nenhum provedor de IA instalado.
